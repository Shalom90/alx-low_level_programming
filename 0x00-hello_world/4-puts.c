#include <stdio.h>
/**
  * main- Execution starts and ends here
  * Return: Returns 0 if no error is found, else return non-zero value
  */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
