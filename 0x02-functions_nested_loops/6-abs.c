#include "main.h"
/**
  * _abs- prints the absolute value of an integer
  * @i: the integer
  * Return: the absolute value of the integer
  */

int _abs(int i)
{
	int n = i;

	if (n >= 0)
	{
		return (n);
	}
		else
		{
			return (-n);
		}

}

