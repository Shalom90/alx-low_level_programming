#include "main.h"
/**
  * add- Adds two integers and returns the result
  *@x: integer one
  *@y: integer two
  *Return: result of the addition of two integers
  */

int add(int x, int y)
{
	return (x + y);
}
